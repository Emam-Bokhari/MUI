import { createTheme } from "@mui/material";

// Create the theme
export const theme = createTheme({
  palette: {
    primary: {
      main: "#FF014F", 
    },
    success: {
      main: "#F3971b", 
    },
    textColor: {
      primary: "orange",
      secondary: "blue",    
    },
  },
});
